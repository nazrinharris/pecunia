/// The key for the map of saved users stored in shared preferences.
const kPrefsSavedUsers = 'saved_users';

/// The key for knowing if the app is opened for the first time.
const kPrefsIsFirstOpen = 'is_first_open';
